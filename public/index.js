letters = queryselector(".letters");

let letter = letters[0];

letter.classlist.toggle("hidden")

for each letter in letters {
  if user input = lettter.textcontent{
    letter.classlist.toggle("hidden")
  }
}
